<div class="container">
  <br>
  <div class="content-container">
  <% if current_user.seller? %>
    <h2>Received offer requests</h2>
    <% @all_offers.each do |offer| %>
      <% if (offer.status == "requested") && (offer.country == current_user.country || offer.country == "") %>
        <div class="card-user">
          <p><b><%= offer.title %></b></p>
          <p><%= link_to "View request", offer_path(offer) %></p>
        </div>
      <% end %>
    <% end %>
  <% else %>
    <h2>Ongoing offer requests</h2>
      <% @offers.each do |offer| %>
        <div class="card-user">
          <p><b><%= link_to offer.title, offer_path(offer) %></b></p>
          <p><b>Available to producers in:</b> <%= offer.country %></p>
          <% if offer.deadline? %>
            <p><b>Deadline:</b> <%= offer.deadline %></p>
          <% end %>
        </div>
      <% end %>
      <div class="button-frame">
        <%= link_to "Create a new request", new_offer_path, class: "btn btn-success" %>
      </div>
  <% end %>
  </div>
</div>



